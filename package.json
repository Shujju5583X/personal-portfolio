{
    "name": "matrix-portfolio-root",
    "version": "1.0.0",
    "scripts": {
        "install": "npm install && cd client && npm install",
        "build": "cd client && npm run build",
        "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
    },
    "dependencies": {
        "express": "^4.18.2",
        "serverless-http": "^3.2.0",
        "cors": "^2.8.5"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}